#

for Mac OSX:
`$pip install gym`
`$brew install boost-python --with-python3`
`$brew install boost-python --with-python3`
`$git clone https://github.com/openai/gym.git`
`$cd gym`
`$pip install -e '.[all]'`
